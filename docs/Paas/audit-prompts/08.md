# Prompt 08 — GTM API routes part 2 (~43 files)

Second half of the GTM API route sweep.

Repo: `/Volumes/ext-data/github/solofame-platform`. Run
`docs/audit-prompts/00.md` first. Follow `CLAUDE.md` audit discipline.

## Scope

Get the full route list:
```bash
find apps/gtm/app/api -name 'route.ts' -o -name 'route.tsx' | sort
```

Take from index 46 to the end (after skipping
`admin/seed-demo`, `webhook/polar`, `webhook/attio` already audited in
Phase 1). Record the exact list in your findings section.

## What to look for

Same as prompt 07 (GTM part 1).

## Deliverables

1. Read every file end-to-end.
2. Findings under `## Slice 08 — GTM API routes part 2`.
3. Ledger: flip every row.
4. Catalog new bug classes.
5. Print ledger delta.

## Rules

No subagents, no narrowing, no self-grading.

# Prompt 15 — GTM _archive + docs + content (~700 files)

Same shape as prompt 06 but for GTM.

Repo: `/Volumes/ext-data/github/solofame-platform`. Run
`docs/audit-prompts/00.md` first. Follow `CLAUDE.md` audit discipline.

## Scope

- `apps/gtm/_archive/` (~511 files)
- `apps/gtm/docs/` (~180 files)
- `apps/gtm/content/` (if present)

## What to look for

Same patterns as prompt 06, plus:

- **`docs/dokploy-conversion/`**: `.gitignore` excludes
  `*_private_id_ed25519*` but verify no SSH keys leaked into other
  filenames.
- **`docs/manuscript/`**: book drafts. Real-world customer examples
  shouldn't be in git.
- **`docs/website-reloaded-2/`**: embedded designer artifacts.

## Deliverables

1. Follow the procedure in prompt 06 (read all small/top-level files;
   bulk grep + drill-down for `_archive`).
2. Findings under `## Slice 15 — GTM archive + docs`.
3. Ledger: flip every row.
4. Print ledger delta.

Note: large slice. Checkpoint + stop if context runs short.

## Rules

No subagents, no narrowing, no self-grading.

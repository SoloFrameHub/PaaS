# Prompt 14 — GTM server/ (engine + data sweep, ~1595 files)

Audit of `apps/gtm/server/**`. Similar structure to DWA's server/ —
mostly research MD and data JSON but some engine code.

Repo: `/Volumes/ext-data/github/solofame-platform`. Run
`docs/audit-prompts/00.md` first. Follow `CLAUDE.md` audit discipline.

## Scope

Total files: ~1595.

### Pass A — executable code (read line-by-line)

```bash
find apps/gtm/server -type f \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' -o -name '*.mjs' -o -name '*.cjs' \) | sort
```

Read every file end-to-end.

### Pass B — data files sweep

Same grep pattern set as prompt 05 (DWA server data sweep), executed
against `apps/gtm/server/data/**` and any other nested content dirs.

## What to look for

Same as prompt 05, plus:

- **research/ markdown** — cold-email research, sales-automation
  research. Check for real customer names, emails, phone numbers that
  shouldn't be in a git tree (PII).
- **course content** — ensure no AWS keys / API tokens accidentally
  pasted into a code-sample block.

## Deliverables

1. Pass A: read all executable files end-to-end.
2. Pass B: grep-sweep all data files; drill into every hit.
3. Findings under `## Slice 14 — GTM server/`.
4. Ledger: bulk-flip data files as clean; re-mark grep-flagged files
   as findings.
5. Print ledger delta.

Note: large slice. If context runs short, checkpoint + stop; a
follow-up chat resumes.

## Rules

No subagents, no narrowing, no self-grading.

# Prompt 18 — Verticals + infra + root configs + docs + .github (~65 files)

Audit of the non-app, non-package surface.

Repo: `/Volumes/ext-data/github/solofame-platform`. Run
`docs/audit-prompts/00.md` first. Follow `CLAUDE.md` audit discipline.

## Scope

- `verticals/**` (16 files — manifests + content references)
- `infra/**` (5 files — migrations, Dockerfile references, dokploy
  state)
- `.github/**` (CI workflows, CODEOWNERS)
- `.changeset/**`
- `docs/**` (21 repo-level docs)
- Root files:
  - `CLAUDE.md`, `README.md`
  - `Dockerfile`, `Dockerfile.ops`
  - `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`
  - `turbo.json`, `tsconfig.base.json`
  - `eslint.config.mjs`, `.dependency-cruiser.cjs`
  - `.dockerignore`, `.gitignore`, `.npmrc`, `.nvmrc`

## What to look for

- **`verticals/*/manifest.json`**: Zod-validated against
  `VerticalManifestZ`. Re-run the loader and confirm. Verify
  `manifest.lock` hashes are consistent with on-disk files.
- **`infra/migrations/*.sql`**: re-read both 0001 and 0002
  line-by-line. Verify the RLS policies cover every tenant-scoped
  table added since.
- **`infra/dokploy/state.json`**: must not contain secrets. Confirm.
- **`.github/workflows/ci.yml`**: any action running with `write-all`
  / `id-token: write` permissions? Any third-party action pinned by
  floating tag instead of SHA?
- **`CODEOWNERS`**: references `@SoloFrameHub/platform-team` which is
  a placeholder. Flag for real team creation.
- **`pnpm-lock.yaml`**: run `pnpm audit` or equivalent; record any
  HIGH/CRITICAL CVEs.
- **Root `Dockerfile` + `Dockerfile.ops`**: already audited in Phase 1
  — line-by-line re-verify.
- **`docs/`** (repo root):
  - `bug-patterns.md` — validate the B-001..B-039 catalog for
    consistency.
  - `source-repo-backports.md` — validate cross-references.
  - `deployment-env.md` — any real secrets or ops IPs?
  - ADRs — re-read each; flag conflicts between stated decision and
    current code.

## Deliverables

1. Read every file end-to-end.
2. Findings under `## Slice 18 — verticals + infra + configs + docs`.
3. Ledger: flip every row.
4. Print ledger delta.

## Rules

No subagents, no narrowing, no self-grading.

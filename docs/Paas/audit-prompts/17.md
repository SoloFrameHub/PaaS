# Prompt 17 — Platform packages + adapters + tools re-verify (~59 files)

Line-by-line re-verify of every file under `packages/`, `adapters/`,
and `tools/` in `/Volumes/ext-data/github/solofame-platform`.

Phase 1 already audited these areas and landed fixes for B-028..B-038.
This slice is a **line-by-line re-verify** — every file end-to-end, no
assumptions carried over.

Run `docs/audit-prompts/00.md` first. Follow `CLAUDE.md` audit
discipline.

## Scope

- `packages/ai-orchestration/src/**` (stub)
- `packages/contracts/src/**`
- `packages/identity/src/**`
- `packages/manifest-loader/src/**` + `test/**`
- `packages/observability/src/**` (stub)
- `packages/prompt-registry/src/**` + `test/**`
- `packages/tenancy/src/**` + `test/**`
- `packages/testing/src/**` + `test/**`
- `packages/ui-primitives/src/**` (stub)
- `packages/ui-shell/src/**` (stub)
- `adapters/*/src/**` (8 adapters, all stubs)
- `tools/dokploy/**` (dk wrapper + 6 provision-*.sh)
- `tools/eslint-plugin-platform/src/**`
- `tools/new-vertical/src/**`
- `tools/platform-ops/**`
- `tools/tenancy/**`
- Each package's `package.json` + `tsconfig.json`

## What to look for

- Every line — not "this was audited in phase 1, skip".
- Re-verify the Phase-1 fixes are intact:
  - B-030 — `getTenantContextFromHeaders` removed.
  - B-031 — `proxy.ts` convention referenced here via the `middleware`
    subpath export.
  - B-032 — `bigserial → bigint` in the tenancy schema.
  - B-033 — `TenantDomainsZ` canonical key.
  - B-034 — `requireTenantContext` fail-closed.
  - B-035 — `no-direct-db-access` ESLint rule breadth.
  - B-037 — `resolveInside` path-traversal guard in manifest-loader.
- Any adapter importing back into `packages/` or `apps/` — Blueprint
  §3.1 forbids this.
- Any stub accidentally shipping real values (e.g., `env.example`
  committed with real keys).
- Every `provision-*.sh` — quoting, command injection,
  `DOKPLOY_API_KEY` handling, temp-file leaks.
- The `dk` wrapper — token visible in `ps` output? logged on error?

## Deliverables

1. Read every file end-to-end.
2. Findings under `## Slice 17 — platform spine re-verify`.
3. Ledger: flip every row.
4. Any divergence from the Phase-1 fixes: escalate immediately.
5. Print ledger delta.

## Rules

No subagents, no narrowing, no self-grading.

# Solo Frame Hub - Error Log & Resolution Database

This document tracks errors encountered during the lesson expansion and system maintenance process, providing resolutions to enable batch fixing and prevent recurrence.

## Logged Errors

### 1. `multi_replace_file_content`: Target Content Not Found
- **Date:** 2025-12-31
- **Step ID:** 232
- **Error Description:** Attempted to replace multiple chunks in `task.md`, but the `TargetContent` strings did not match the file content exactly (likely due to whitespace or trailing newline differences).
- **Resolution:** Perform a fresh `view_file` call to capture the exact character sequence, or use `replace_file_content` for larger contiguous blocks to reduce matching complexity.

### 2. `view_file`: File Not Found (foundations/list-building/lesson-12.md)
- **Date:** 2025-12-31
- **Step ID:** 257
- **Error Description:** Attempted to access `lesson-12.md` in `foundations/list-building` based on perceived curriculum requirements, but the file did not exist on disk.
- **Resolution:** Ran `list_dir` to audit the directory. Discovered Course 4 only had 11 lessons. Fixed by consolidating the intended "CRM Fundamentals" content into Lesson 11 to ensure a complete course experience without requiring a new file creation that might break sidebar routing.

### 3. Sidebar Course/Track Number Stripping
- **Date:** 2025-12-31
- **Context:** UI Bug
- **Error Description:** The sidebar was stripping numbers from track and course titles (e.g., "1. Foundations" became "Foundations") due to `.split(': ')[1]` logic in `components/ui/sidebar.tsx`.
- **Resolution:** Modified `sidebar.tsx` to stop splitting the title strings, ensuring the full numbered title is displayed.

### 4. Word Count Script: Aggressive Regex Erasure
- **Date:** 2025-12-31
- **Step ID:** 331
- **Error Description:** The `analyze-lesson-word-counts.js` script was undercounting words because its YAML frontmatter detector used the `m` (multiline) flag, which caused it to delete all text between any two `---` dividers (common in markdown) as if it were frontmatter. It also used an overly greedy JSON detector that wiped out prose.
- **Resolution:** Modified the script to only remove `---` blocks if they occur at the very beginning of the file (index 0). Removed the aggressive JSON regex as code blocks are already handled.

### 5. `replace_file_content`: String Mismatch on Expanded Files
- **Date:** 2025-12-31
- **Step ID:** 315
- **Error Description:** Attempted to expand `foundations/list-building/lesson-11.md` but failed because the `TargetContent` didn't match. This happens frequently when attempting to replace large blocks that have been modified by previous edits or have complex whitespace.
- **Resolution:** Use `view_file` to verify the state of the file *immediately* before the edit, and ensure the `TargetContent` is a unique, clear anchor.

### 6. `next lint` Failure: Invalid Project Directory
- **Date:** 2025-12-31
- **Step ID:** 424
- **Error Description:** Running `npm run lint` or `next lint` fails with `Invalid project directory provided, no such directory: /Volumes/ext-data/github/solo-founder-sales-academy/soloframehub-v2/lint`. It seems Next.js is misinterpreting the command or environment.
- **Resolution:** Pending. Likely requires checking `next.config.js` or `.eslintrc.json` for path misconfigurations, or specifying the directory explicitly (though `next lint .` also failed).

## Identified Page Access & Architectural Issues (Step 519-587)

- **Issue:** `app/academy` directory does not exist.
  - **Context:** Routing in Next.js uses Route Groups. The actual path is `app/(default)/academy/`.
  - **Impact:** Failed `find_by_name` and potential developer confusion when navigating the file system.
  - **Resolution:** Acknowledge route group structure.

- **Issue:** Lesson Content Filename Inconsistency.
  - **Context:** `lib/lessons.ts` expects filenames like `lesson-${lessonId}.md`.
  - **Observation:** Some lessons in `server/data/content` might not follow this pattern accurately.
  - **Impact:** Dynamic rendering fails if filenames don't match `${lessonId}`.

- **Issue:** Directory Naming Mismatch (Track IDs).
  - **Context:** `curriculum.ts` defines track IDs like `marketing-engine`, but some content exists in `/server/data/content/marketing/`.
  - **Impact:** `getTrackIdForCourse` returns IDs that might not match the physical directory structure.

- **Issue:** Orphaned lesson file `sales-methodology/discovery-framework/lesson-13.md` detected.
  - **Context:** `curriculum.ts` only defines 12 lessons for Course 14 (Discovery Framework).
  - **Impact:** This lesson is not reachable via the UI and causes confusion during automated analysis.
  - **Resolution:** (Draft) Move relevant content from lesson-13 into lesson-12 or update curriculum definition if a 13th lesson is intended.

---
*Generated by Antigravity AI Assistant*
